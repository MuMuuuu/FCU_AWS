# Git usage

## Update file
`git add ./<file_name>`

## Commit message
`git commit -m "<message>"`

## Git push to remote
`git push origin main`

## Sync with remote
`git pull origin main`

## Watch status
`git status`

